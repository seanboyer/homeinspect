<h1>Contact#index</h1>
<p>Find me in app/views/contact/index.html.erb</p>


<% @contacts.each do |contact| %>
<div>
	<p>
		<%= contact.created_at.strftime("%b-%d-%Y") %> | <%= link_to contact.name, contact_path(contact) %>
	</p>
</div>
<% end %>