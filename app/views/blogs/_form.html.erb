<%= simple_form_for(@blog) do |f| %>
  <% if @blog.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@blog.errors.count, "error") %> prohibited this blog from being saved:</h2>

      <ul>
      <% @blog.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.input :title, :input_html => {:minlength =>2,:style=> 'width: 400px', class: "form-control"} %>
  </div>
  <div class="form-group">
    <%= f.input :content, label: 'Post Content', :input_html => {:style=> 'width: 400px; height: 400px', class: "form-control"} %>
  </div>
  <div class="actions">
    <%= f.button :submit, 'Save', :class => "btn btn-default"  %>
    <%= f.button :submit, 'Save and Post', :class => "btn btn-default"  %>
  </div>
<% end %>
